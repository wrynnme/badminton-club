/**
 * Primary brand color as a hex string. Used by metadata `themeColor` (viewport)
 * and the PWA manifest `theme_color`.
 *
 * Keep in sync with `--primary` in globals.css — sRGB of oklch(0.488 0.243 264.376).
 */
export const BRAND_THEME_COLOR = "#1447E6";
